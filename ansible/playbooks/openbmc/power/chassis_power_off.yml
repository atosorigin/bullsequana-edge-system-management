---
- hosts: all
  connection: local
  name: Manage Chassis Power - Turn off
  gather_facts: False
  vars_files:
    - /var/lib/awx/projects/vars/external_vars.yml
    - /var/lib/awx/projects/vars/passwords.yml

  tasks:
  - include_tasks: ../utils/utils_create_token.yml

  - include_tasks: ../utils/utils_chassis_power_off.yml

  - name: Count down "{{poweroff_countdown}}" seconds before checking power off
    pause:
      seconds: "{{poweroff_countdown}}"

  - include_tasks: ../utils/utils_check_power_off.yml    
 

