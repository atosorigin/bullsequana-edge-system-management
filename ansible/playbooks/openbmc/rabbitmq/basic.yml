---
- hosts: openbmc
  connection: local
  name: Basic Rabbitmq test
  gather_facts: False

  tasks:
  - name: RabbitMQ auto generated queue
    rabbitmq_publish:
      url: "amqp://awx:awxpass@rabbitmq:5672/redfish"
      queue: redfishmq
      durable: true
      body: "Hello world random queue from ansible module rabbitmq_publish"
      content_type: "text/plain"
